/**
 * File tct-player.js
 *
 */

// PLAYER INIT
/*
var tct_options = {
	background: true,
	responsive: true
};

var iframe = document.querySelector( 'iframe' );
var tct_player = new Vimeo.Player( iframe, 'tct_options' );

tct_player.on('play', function() {
	console.log('Played the video');
});


tct_player.play();
tct_player.setAutopause( false );


tct_player.getVideoTitle().then( function( title ) {
	console.log( 'title:', title );
});
*/

